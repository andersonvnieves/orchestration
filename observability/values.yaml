grafana:
  enabled: true
  adminPassword: "admin" # Troque para uma senha segura depois
  service:
    type: ClusterIP # Ou LoadBalancer / NodePort dependendo de como você acessa o cluster

prometheus:
  prometheusSpec:
    retention: 7d # Tempo que as métricas ficam salvas (ajuste conforme seu disco)
    resources:
      requests:
        cpu: 500m
        memory: 512Mi