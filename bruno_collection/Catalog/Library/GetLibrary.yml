info:
  name: GetLibrary
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseURL}}api/library"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
