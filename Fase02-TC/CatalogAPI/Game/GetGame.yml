info:
  name: GetGame
  type: http
  seq: 2

http:
  method: GET
  url: "{{CatalogBaseURL}}api/Game?id={{GameId}}"
  params:
    - name: id
      value: "{{GameId}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
