info:
  name: Login as user
  type: http
  seq: 3

http:
  method: POST
  url: "{{UserBaseURL}}api/auth/login"
  body:
    type: json
    data: |-
      {
        "email": "nieves@gmail.com",
        "password": "SuperSenha@2026"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        const response = res.getBody();

        bru.setEnvVar("accessToken", response.token);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
